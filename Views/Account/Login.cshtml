@{
    ViewData["Title"] = "Login";
}

<div class="container mt-5" style="max-width:400px;">
    <h3 class="text-center mb-4">Login</h3>

    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger">@ViewBag.Error</div>
    }

    <form method="post" asp-action="Login" asp-controller="Account">
        <div class="form-group mb-3">
            <label for="username">Username</label>
            <input type="text" class="form-control" id="username" name="username" placeholder="Enter username" required />
        </div>

        <div class="form-group mb-3">
            <label for="password">Password</label>
            <input type="password" class="form-control" id="password" name="password" placeholder="Enter password" required />
        </div>

        <button type="submit" class="btn btn-primary w-100">Login</button>
    </form>

    <hr />

    <div class="mt-3">
        <h6>💡 Test accounts:</h6>
        <ul>
            <li><b>Coordinator</b> → username: <code>coordinator</code>, password: <code>123</code></li>
            <li><b>Manager</b> → username: <code>manager</code>, password: <code>123</code></li>
            <li><b>Lecturer</b> → username: <code>lecturer</code>, password: <code>123</code></li>
        </ul>
    </div>
</div>
